# 引き継ぎメモ（Claude Code向け）

このファイルは、チャットで作った `index.html` を Claude Code で続けて開発するための引き継ぎです。
作業を始める前にこれを読ませてください。

---

## 0. これは何か

モンハンワイルズ用の個人ツール。**HTML 1枚**で完結していて、ビルドもnpmも使っていません。
`index.html` をブラウザで開けば動きます。GitHub Pages にそのまま置けます。

- リポジトリ: https://github.com/hts71321-droid/MHR.-simulator
- 公開URL（Pages有効化後）: https://hts71321-droid.github.io/MHR.-simulator/

**作者はプログラミング専門ではありません。**生成AIを使って作っています。
コードを変更したら、何をどう変えたかを日本語で説明してください。専門用語には一言添えると助かります。

---

## 1. ファイル構成

```
/
├─ index.html          ← 全部これ。HTML + CSS + JS が1ファイルに入っている
├─ README.md           ← 使い方と公開手順
├─ HANDOFF.md          ← このファイル
└─ tools/
   └─ build-data.js    ← DATA ブロックを作り直すスクリプト（ページには含まれない）
```

`tools/` はページの一部ではなく、データ更新のときだけ使う道具です。
**1ファイル構成という約束は `index.html` の話**なので、ここに道具を置いても崩れません。

**分割しないでください。** 1枚で完結していることが、この人にとっての扱いやすさです。
どうしても分けたくなったら、先に相談してください。

---

## 2. index.html の中の構造

上から順に:

| 位置 | 中身 |
| --- | --- |
| `<style>` | CSS。CSS変数（`--ink` `--ember` など）で色を管理 |
| `<body>` | 4つの `<section>` = 4タブ（アーティア工房 / 強化テーブル / 未来視テーブル / 装備シミュレータ） |
| `<script>` 前半 | **データ定義**。`ARTIAN` `SERIES_SKILLS` `GROUP_SKILLS` `DATA` |
| `<script>` 後半 | **ロジック**。データの中身をハードコードしていない |

**設計の要**: データとロジックが分かれています。ゲームのアプデで数値が変わったら
データ定数だけ直せば済むように作ってあります。この分離は壊さないでください。

### 主要なデータ構造

```js
ARTIAN = {
  base:  {atk:190, aff:5, slots:"3-3-3"},   // レア8アーティアの素の値
  step:  {atk:5, aff:5},                     // 増強1個あたりの上がり幅
  caps:  {"基礎攻撃力強化":5, "会心率強化":3, "属性強化":4, "切れ味強化":2, "装填数強化":2},
  types: { "太刀": {parts:[...], table:"elem", melee:true}, ... },  // 14武器種
  tableOf(type, elem)   // → "太刀2" のようなテーブル名を返す
  mates(type, elem)     // → 同じテーブルになる属性の配列（捨て武器用）
  bonusesOf(type, elem) // → その組み合わせで出る復元ボーナスの配列
  variants: {...}       // 武器種ごとの弾追加・ビン・砲撃型・旋律パターン
  recommend: {...}      // 定番の復元構成（文字列）
}

SERIES_SKILLS = { "巨戟龍の黙示録": {2:"…", 4:"…"}, ... }   // 25種
GROUP_SKILLS  = { "ヌシの魂": {3:"…"}, ... }                // 17種

futures = { 武器種: { 属性: [ {g:シリーズ名, s:グループ名}, ... ] } }
//                                  ↑ 配列の添字が「生産回数 - 1」
//   ※ g がシリーズ、s がグループ。命名が紛らわしいので触るとき注意。
//      元Excelの列順（グループ列が左）に引きずられた名残です。

want = { series:[…], group:[…], mode:"and"|"or", only:bool }  // 当たり条件
```

### localStorage（v2 = ID別保存）

ID（プロファイル）ごとに引き出しを分けています。キーは2種類:

| キー | 中身 |
| --- | --- |
| `mhw-artian-v2` | `{current, profiles:[{id,name,at}]}` — IDの一覧と、選択中のID |
| `mhw-artian-v2:<id>` | `{futures, want, picked, records, build, grant, boosts, bwant}` — そのIDの中身 |

**項目を足すだけならキーを上げなくて構いません。** `restore()` が無い項目を初期値で埋めるので、
古い保存もそのまま読めます（`boosts` / `bwant` はこの方式で後から足しました）。
上げるのは、既存の項目の意味や形が変わるときだけです。

**IDごとに別キーにしているのは意図的です。** 未来視テーブルは1マス変えるたびに保存が走るので、
1キーにまとめると全ID分を毎回シリアライズすることになり、表が大きいほど重くなります。

初回起動時に `mhw-artian-v1` があれば、最初のIDへ自動で引き継ぎます（`loadLocal()` 内）。
v1のキー自体は消していないので、取り込みが失敗しても元データは残ります。

`readJSON()` / `writeJSON()` は try/catch で握りつぶしているので、読めなくても動作は止まりません。
書き込みに失敗した場合（容量超過など）だけ、`saveLocal()` が一度きりアラートを出します。

#### 状態変数と保存の対応

`snapshot()` が保存する側、`restore()` が戻す側です。**状態変数を増やしたら両方に足してください。**
片方だけだとID切り替えで前のIDの値が残ります。`redrawAll()` は切り替え後の全画面再描画です。

---

## 3. ドメイン知識（これを知らないとバグを入れます）

### アーティアのテーブル

- テーブルは **武器種 × 属性** で決まる。基本は「無属性 = テーブル1」「属性付き = テーブル2」
- 例外1: **スラアク・ライトボウガン・ヘビィボウガン** はテーブルが1つだけ（属性で分かれない）
- 例外2: **弓** は「無/毒/麻痺/睡眠 = 弓1」「火水雷氷龍/爆破 = 弓2」という変則
- 同じテーブルの属性は**捨て武器**に使える（本命のパーツを消費せずテーブルを進められる）
- **同じ武器種なら、どの属性を作っても生産回数のカウントは共通で進む**
  → これが未来視テーブルを横並びにしている理由。武器種が違うとカウントは別々

### 復元ボーナス

5枠。武器種と属性で**選択肢そのものが変わります**:
- ボウガン系 → 属性強化・切れ味強化が出ない、代わりに装填数強化
- 無属性の近接 → 属性強化が出ない（ただしスラアクは無属性でも出る）
- 弓の弓1テーブル → 属性強化が出ない

### 巨戟アーティア

レア8アーティアをゴグマジオス素材で強化した形態。
**シリーズスキルとグループスキルが1つずつランダムで付き、素材で再抽選できます。**
未来視テーブルは、この再抽選が何回目に何が出るかを記録するためのものです。

### 防具データの扱い

`DATA.armors` / `DATA.decos` / `DATA.charms` / `DATA.skills` は
[mhdb-wilds-data](https://github.com/LartTyler/mhdb-wilds-data) から取り込んだ実データです
（取り込み時点のコミット `c50a1eb` / 2026-04-14、リポジトリのライセンスは GPL-3.0）。
**手で直さず、`tools/build-data.js` で作り直してください。** 照合まで自動でやります。

大事な仕様が2つあります:

- **装飾品には装着先があります。** `on:"weapon"` は武器スロット専用、`on:"armor"` は防具スロット専用です。
  `decoPool()` がこれで振り分けています。混ぜると実機で入らない組み合わせが出せてしまいます
- **装備は「並び順の番号」ではなく「名前」で保存しています**（`build[部位].name`）。
  番号で持つと、データを更新しただけで保存済みの構成が別の装備に化けます。
  `restore()` は昔の番号形式を読み捨てるようにしてあります

### 用語の注意

作者が参照している攻略コミュニティのExcelでは、**「グループ」列にシリーズスキル、
「シリーズ」列にグループスキル**が入っていることがあります（ゲーム内の定義と逆）。
貼り付け取り込みの `parsePaste()` は、名前の当たり具合を数えて自動で判定しています。
ここを"直さないで"ください。仕様です。

---

## 4. 今の状態

### 動いているもの

- ✅ アーティア工房: テーブル判定、復元ボーナスの選択肢と上限、攻撃力/会心率の計算、
      武器種固有の弾追加・ビン・砲撃型・旋律、厳選記録（テーブル別・何本目か）
- ✅ 未来視テーブル: 武器種を複数選んで横並び、全セルがプルダウン、
      当たりスキルの指定（AND/OR）、当たり行だけ表示、Excel貼り付け取り込み、
      略称の名寄せ（「獄焔蛸」→「獄焔蛸の反逆」）、JSON書き出し/読み込み、自動保存
- ✅ 装備シミュレータ: スキルLv集計、スロットサイズに応じた装飾品の絞り込み、
      シリーズ2/4部位・グループ3部位の判定、巨戟の付与スキル合流。
      **実データ入り**（防具714部位／装飾品361／護石183／通常スキル137）。
      防具名・スキル名・シリーズ名での絞り込み欄あり
- ✅ 強化テーブル: 復元ボーナスの並びを「テーブル名 → 回数 → 5枠」で記録。
      行ごとの個数集計、当たり内訳の指定（○個以上）、枠ごとの出方の集計、Excel貼り付け取り込み。
      **キーは武器種ではなくテーブル名**です（並びはテーブル単位で決まるため）
- ✅ ID管理（端末内プロファイル）: ヘッダ右の `.idbar`。新規／名前／複製／削除／書出／読込。
      未来視テーブル・厳選記録・装備構成をIDごとに保存。v1データの自動引き継ぎ付き。
      **サーバーは使っていません。**端末をまたぐ共有はJSONの書出／読込で行う前提です

### 未完（次にやること）

1. **`DATA.weapons` がサンプル2件のまま** ← 最優先
   アーティア以外の武器を選べません。mhdb-wilds-data の `output/merged/weapons/` に
   武器種ごとのJSONがあるので、`tools/build-data.js` を伸ばせば取り込めます。

2. 防具が714部位あるので、部位ごとの `<select>` が長いです。
   絞り込み欄は付けましたが、シリーズ単位でまとめて着せ替える機能があると楽かもしれません。

### 既知の制約

- ID管理は**その端末のそのブラウザの中だけ**です。パスワードもサーバーもありません。
  ブラウザの履歴削除やシークレットウィンドウでは消えます。
  スマホとPCで同じ記録を見たくなったら、外部のデータベース（Supabase など）を足す必要があります
- 武器種を5種類以上並べるとセルが数千個になり、描画が重くなります。
  今は毎回 `drawFut()` で全再描画しているので、重くなったら差分更新への作り替えが要ります
- 数値の出典は有志の検証記事です。実機と1〜2ズレる可能性があります。
  ズレたら `ARTIAN.base` / `ARTIAN.step` を直せば追随します

---

## 5. 作業のときの約束

- **artifacts の localStorage 制限は関係ありません。**これは普通のWebページなので使えます
- 変更したら、必ず**ブラウザで開いて動作確認**してください。
  最低限、構文チェックは:
  ```bash
  node -e "const s=require('fs').readFileSync('index.html','utf8');
    new Function(s.split('<script>')[1].split('</'+'script>')[0]); console.log('OK')"
  ```
- UIの文言は**日本語**、常体ではなく丁寧語寄りで統一しています
- 色は CSS変数を使ってください。直接カラーコードを書かない
- 見た目の方向性: 暗い鉄と砂色、明朝体の見出し、装飾を足さない。
  ゲームの世界観に寄せつつ、データが読みやすいことを優先しています

---

## 6. すぐ使えるプロンプト例

> index.html の DATA.armors に、以下の防具データを追加して。
> series と group は SERIES_SKILLS / GROUP_SKILLS のキーと完全一致させること。
> （※ここに攻略サイトからコピーした表を貼る）

> 未来視テーブルの描画が重い。drawFut() を全再描画ではなく、
> 変わったセルだけ更新する形に直せる？既存の挙動は変えないで。

> ARTIAN.base.atk を実機の値に合わせたい。今190だけど、
> ゲーム内表示だと◯◯だった。関連する箇所を全部直して。
